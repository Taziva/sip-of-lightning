# Migration `20210117165138-keystone-ckk1dmve10000ebyibzgy5aab`

This migration has been generated by Taziva at 1/17/2021, 4:51:38 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "Comment" (
"id" SERIAL,
    "body" TEXT,
    "posted_utc" TIMESTAMP(3),
    "posted_offset" TEXT,
    "originalPost" INTEGER,
    "author" INTEGER,

    PRIMARY KEY ("id")
)

ALTER TABLE "Comment" ADD FOREIGN KEY("originalPost")REFERENCES "Post"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "Comment" ADD FOREIGN KEY("author")REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20210117163448-keystone-ckk1d18ee00004ayi5gyo7y41..20210117165138-keystone-ckk1dmve10000ebyibzgy5aab
--- datamodel.dml
+++ datamodel.dml
@@ -1,6 +1,6 @@
 datasource postgresql {
-  url = "***"
+  url = "***"
   provider = "postgresql"
 }
 generator client {
@@ -8,14 +8,15 @@
   output   = "generated-client"
 }
 model User {
-  id       Int      @id @default(autoincrement())
+  id       Int       @id @default(autoincrement())
   name     String?
-  email    String?  @unique
+  email    String?   @unique
   isAdmin  Boolean?
   password String?
-  Post     Post[]   @relation("Postauthor")
+  Post     Post[]    @relation("Postauthor")
+  Comment  Comment[] @relation("Commentauthor")
 }
 model Post {
   id                 Int            @id @default(autoincrement())
@@ -33,10 +34,22 @@
   createdAt_offset   String?
   author             User?          @relation("Postauthor", fields: [authorId], references: [id])
   authorId           Int?           @map("author")
   categories         PostCategory[] @relation("Post_categories_many", references: [id])
+  Comment            Comment[]      @relation("CommentoriginalPost")
 }
+model Comment {
+  id             Int       @id @default(autoincrement())
+  body           String?
+  posted_utc     DateTime?
+  posted_offset  String?
+  originalPost   Post?     @relation("CommentoriginalPost", fields: [originalPostId], references: [id])
+  originalPostId Int?      @map("originalPost")
+  author         User?     @relation("Commentauthor", fields: [authorId], references: [id])
+  authorId       Int?      @map("author")
+}
+
 model PostCategory {
   id              Int     @id @default(autoincrement())
   name            String?
   slug            String? @unique
```


